#include <iostream>

using namespace std;

int main() {
    double threshold;
    double piVal = 0;
    cout << "Threshold of decimal places for Pi (ex. 0.0003): " << endl();
    cin >> threshold;

    return 0;
}
